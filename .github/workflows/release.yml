name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-macos:
    # macos-14 = Apple Silicon (M1). We cross-compile for x86_64 from here
    # because GitHub no longer offers free Intel (macos-13) runners.
    runs-on: macos-14
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add x86_64 cross-compilation target
        run: rustup target add x86_64-apple-darwin

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Node dependencies
        run: npm ci

      # ── arm64 (native) ─────────────────────────────────────────────────────

      - name: Build arm64 app bundle
        run: npm run tauri build -- --bundles app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Sign arm64 bundle
        run: |
          codesign --deep --force --sign - \
            --entitlements src-tauri/entitlements.plist \
            "src-tauri/target/release/bundle/macos/Voz Local.app"

      - name: Create arm64 DMG
        run: |
          VERSION=$(grep '^version' src-tauri/Cargo.toml | head -1 | cut -d'"' -f2)
          mkdir -p dist
          hdiutil create \
            -volname "Voz Local" \
            -srcfolder "src-tauri/target/release/bundle/macos/Voz Local.app" \
            -ov -format UDZO \
            "dist/Voz Local_${VERSION}_aarch64.dmg"

      # ── x86_64 (cross-compiled from arm64) ─────────────────────────────────

      - name: Build x86_64 app bundle (cross-compiled)
        run: npm run tauri build -- --target x86_64-apple-darwin --bundles app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Sign x86_64 bundle
        run: |
          codesign --deep --force --sign - \
            --entitlements src-tauri/entitlements.plist \
            "src-tauri/target/x86_64-apple-darwin/release/bundle/macos/Voz Local.app"

      - name: Create x86_64 DMG
        run: |
          VERSION=$(grep '^version' src-tauri/Cargo.toml | head -1 | cut -d'"' -f2)
          hdiutil create \
            -volname "Voz Local" \
            -srcfolder "src-tauri/target/x86_64-apple-darwin/release/bundle/macos/Voz Local.app" \
            -ov -format UDZO \
            "dist/Voz Local_${VERSION}_x86_64.dmg"

      # ── Publish release ─────────────────────────────────────────────────────

      - name: Upload DMGs to Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Voz Local ${{ github.ref_name }}"
          body: |
            ## Descarga

            Elige el DMG para tu Mac:
            - **Apple Silicon (M1/M2/M3/M4):** `Voz Local_*_aarch64.dmg`
            - **Intel Mac:** `Voz Local_*_x86_64.dmg`

            ## Instalación

            1. Abre el `.dmg` y arrastra **Voz Local** a Aplicaciones
            2. **Primer arranque:** clic derecho → Abrir (la app no está notarizada)
            3. Concede permisos de **Micrófono** y **Accesibilidad**
            4. Descarga un modelo Whisper desde la pantalla de bienvenida
            5. Pulsa `Alt+Espacio` para empezar a dictar

            > Si macOS dice que la app está dañada: `sudo xattr -cr /Applications/Voz\ Local.app`

            > Requiere macOS 11 (Big Sur) o superior · Apple Silicon e Intel
          files: dist/*.dmg
          draft: false
          prerelease: false
